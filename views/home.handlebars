{{#if allNotes.length}}
<ul>
  {{#each allNotes}}
  <li>
    {{this.title}}
    {{this.content}} 
  </li>
  {{/each}}
</ul>

{{else}}
<p class="not-found">We're sorry, but no results were found for {{name}}.</p>
{{/if}}